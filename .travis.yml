language: node_js
node_js:
#  - "node"
  - "7"
#  - "6"
#  - "5"
#  - "4"
install:
  - true
script:
  - true
before_deploy:
  - ls -la
  - zip -r latest * && mkdir deploy_zip && mv latest.zip deploy_zip/
deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY
    secret_access_key: $AWS_SECRET_KEY
    local_dir: deploy_zip
    skip_cleanup: true
    bucket: nervous-efficient-rebel
    region: eu-west-1
    on: &2
      branch: develop
  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY
    secret_access_key: $AWS_SECRET_KEY
    key: latest.zip
    bundle_type: zip
    bucket: nervous-efficient-rebel
    application: nervous-efficient-rebel-app
    deployment_group: nervous-efficient-rebel-depgrp
    region: eu-west-1
    wait-until-deployed: true
    on: *2

branches:
  except:
    - /v\d+\.\d+\.\d+/
notifications:
  email: false

